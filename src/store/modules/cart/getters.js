export default {
    cartItems( state ){
        return state.cartItems
    },
    getCartTotal( state ) {
        return state.cartTotal
    },
    getOrderDuration( state ) {
        return state.orderDuration
    }
}